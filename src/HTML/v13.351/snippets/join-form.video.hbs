    <video
        id="join-background-video"
        autoplay
        loop
        muted
        playsinline
        src="{{world.background}}"
        onerror="this.remove()"
        style="position: fixed; inset: 0; width: 100vw; height: 100vh; object-fit: cover; z-index: -1; pointer-events: none;"
    ></video>
